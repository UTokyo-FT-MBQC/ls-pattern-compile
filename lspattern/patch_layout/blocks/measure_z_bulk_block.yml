name: MeasureZBulkBlock
description: |
  This block represents a measure Z bulk block. The terminate timing is synchronized with short z memory block.

layers: # list of layers in this block
  - type: MemoryUnit
    num_layers_from_distance: rest
  - type: MeasureZShiftedUnit
    num_layers: 1
